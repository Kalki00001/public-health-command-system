services:
  - type: web
    name: public-health-command-system
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn realtime_backend:app --host 0.0.0.0 --port $PORT
